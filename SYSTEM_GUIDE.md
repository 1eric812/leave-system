# 请假审批流系统 - 前端应用

## 项目概述

这是一个面向中小企业的员工请假管理系统前端应用。系统实现了完整的请假申请、两级审批流程、请假记录管理等功能，采用 Vue 3 + Element Plus + Day.js 的技术栈。

## 核心功能

### 1. 请假申请（LeaveApplication）
- 员工可提交请假申请
- 支持多种假期类型：年假、病假、婚假、丧假、其他
- 自动计算请假天数
- 自动判断审批级别（≤3天一级，>3天二级）
- 提交前可编辑，待审时可修改

### 2. 审批流管理（ApprovalFlow）
- 部门经理（一级审批）：审批所有请假申请
- 总经理（二级审批）：复审超过3天的申请
- 支持批准或驳回操作，需要填写备注
- 驳回后无法修改，需要重新提交

### 3. 请假记录（LeaveRecords）
- 查看个人请假历史
- 统计请假数据：总请假数、已批准数、待审批数、已驳回数
- 支持按状态和假期类型筛选
- 可在待审时编辑或删除

## 技术栈

- **Vue 3**：最新的前端框架，使用 `<script setup>` 语法
- **Element Plus**：企业级 UI 组件库
- **Pinia**：Vue 3 推荐的状态管理（已嵌入 App.vue）
- **Day.js**：轻量级日期处理库
- **Vite**：新一代前端构建工具

## 项目结构

```
src/
├── App.vue                      # 主应用组件（包含状态管理）
├── components/
│   ├── LeaveApplication.vue     # 请假申请表单
│   ├── ApprovalFlow.vue         # 审批流管理页面
│   └── LeaveRecords.vue         # 请假记录页面
├── main.js                      # 应用入口
└── style.css                    # 全局样式
```

## 安装和运行

### 1. 安装依赖

```bash
npm install
```

### 2. 开发模式运行

```bash
npm run dev
```

然后打开浏览器访问 `http://localhost:5173`

### 3. 生产构建

```bash
npm run build
```

### 4. 预览生产构建

```bash
npm run preview
```

## 使用说明

### 系统用户

系统预置了4个测试用户，可通过顶部用户菜单切换身份：

| 用户ID | 姓名 | 部门 | 角色 | 权限 |
|--------|------|------|------|------|
| emp001 | 张三 | 技术部 | 员工 | 提交申请 |
| emp002 | 王五 | 销售部 | 员工 | 提交申请 |
| mgr001 | 李四 | 技术部 | 部门经理 | 一级审批 |
| mgr002 | 赵六 | 行政部 | 总经理 | 二级审批 |

### 操作流程

#### 员工操作流程：
1. 点击"提交申请"进入请假申请页面
2. 选择假期类型、开始/结束日期、填写请假原因
3. 系统自动计算请假天数和审批级别
4. 点击"提交申请"发送审批
5. 在"我的请假"页面查看审批进度

#### 经理审批流程：
1. 切换到经理身份（在用户菜单选择"切换身份"）
2. 点击"审批流程"查看待审请假申请
3. 点击申请卡片查看详情
4. 填写审批意见，点击"批准"或"驳回"

### 业务规则

1. **两级审批机制**：
   - 请假天数 ≤ 3 天：仅需一级审批（部门经理批准后直接通过）
   - 请假天数 > 3 天：需要二级审批（部门经理批准后，由总经理复审）

2. **编辑规则**：
   - ✅ 提交前：可修改任何内容
   - ✅ 待审批状态：申请人可修改
   - ❌ 已批准状态：不可修改
   - ❌ 已驳回状态：不可修改（需要重新提交新申请）

3. **驳回处理**：
   - 驳回后请假单状态为最终状态
   - 申请人可查看驳回原因
   - 需要重新提交新的请假申请

## 数据模型

### LeaveRequest（请假申请）
```javascript
{
  id: string,                    // 唯一 ID
  userId: string,               // 申请人 ID
  userName: string,             // 申请人名字
  dept: string,                 // 部门
  leaveType: string,            // 假期类型
  startDate: Date,              // 开始日期
  endDate: Date,                // 结束日期
  duration: number,             // 天数
  reason: string,               // 请假原因
  status: string,               // pending | approved | rejected
  approvalLevel: number,        // 1 | 2 审批级别
  approvalHistory: Array,       // 审批历史
  createdAt: Date,              // 创建时间
  updatedAt: Date,              // 更新时间
}
```

### ApprovalRecord（审批记录）
```javascript
{
  id: string,
  requestId: string,            // 申请 ID
  approverId: string,           // 审批者 ID
  approverName: string,         // 审批者名字
  action: string,               // approve | reject
  reason: string,               // 批准/驳回原因
  approvalLevel: number,        // 当前审批级别
  timestamp: Date,              // 审批时间
}
```

## 功能演示

### 场景1：员工提交3天年假申请
1. 员工张三切换到自己的账号
2. 点击"提交申请"
3. 选择"年假"，选择日期（假设3天）
4. 填写原因"回家休息"
5. 点击提交，系统显示"一级审批"
6. 部门经理李四在审批流程中看到该申请
7. 经理点击批准，申请变为"已批准"

### 场景2：员工提交5天病假申请
1. 员工王五提交5天病假
2. 系统自动判断需要"二级审批"
3. 部门经理李四先进行一级审批
4. 批准后，总经理赵六看到该申请
5. 赵六进行二级审批
6. 最终状态变为"已批准"

### 场景3：请假被驳回
1. 员工提交申请
2. 经理驳回，并填写驳回原因
3. 申请状态变为"已驳回"
4. 员工可在"我的请假"查看驳回原因
5. 员工需要重新提交新的申请

## 存储说明

当前系统使用内存存储模拟数据，刷新页面后数据会重置。在实际生产环境中，需要：

1. 连接后端 API 获取数据
2. 实现实时数据同步
3. 集成实际的数据库存储

## 样式定制

系统使用 Element Plus 的默认主题。如需修改样式，可以：

1. 修改 `src/style.css` 中的全局样式
2. 在各组件的 `<style scoped>` 中添加自定义样式
3. 在 `main.js` 中导入自定义主题

## 常见问题

### Q: 如何连接后端 API？
A: 在 `App.vue` 中的 `createLeaveStore()` 函数内，将本地操作替换为 API 调用即可。例如，使用 axios 库来发送 HTTP 请求。

### Q: 如何导出请假记录？
A: 可以在 LeaveRecords 组件中添加导出功能，使用库如 `xlsx` 或 `papaparse` 来生成 Excel 或 CSV 文件。

### Q: 支持多语言吗？
A: 当前系统仅支持中文。如需多语言支持，可集成 `vue-i18n` 库。

## 许可证

本项目是大学课程设计项目，仅供学习使用。

## 作者

学生课程作业 - 请假审批流系统前端实现
