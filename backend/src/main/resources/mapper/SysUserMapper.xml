<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.leave.mapper.SysUserMapper">

    <!-- 根据ID查询用户 -->
    <select id="selectById" resultType="com.example.leave.entity.SysUser">
        SELECT id, name, dept, role, level, password, created_at, updated_at
        FROM sys_user
        WHERE id = #{id}
    </select>

    <!-- 根据部门和角色查询用户 -->
    <select id="selectByDeptAndRole" resultType="com.example.leave.entity.SysUser">
        SELECT id, name, dept, role, level, password, created_at, updated_at
        FROM sys_user
        WHERE dept = #{dept} AND role = #{role}
            LIMIT 1
    </select>

</mapper>