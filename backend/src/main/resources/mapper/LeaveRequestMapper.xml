<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.leave.mapper.LeaveRequestMapper">

    <!--
        TODO: 请假申请 SQL 映射
        - 查询请假单（按ID、用户、部门、状态、日期范围）
        - 分页查询
        - 新增请假申请
        - 更新请假单状态
        - 更新审批级别
    --><select id="selectPage" resultType="com.example.leave.entity.LeaveRequest">
    SELECT id, user_id, user_name, dept, leave_type, start_date, end_date,
    duration, reason, status, approval_level, created_at, updated_at
    FROM leave_request
    <where>
        <if test="ew.entity.userId != null and ew.entity.userId != ''">
            AND user_id = #{ew.entity.userId}
        </if>
        <if test="ew.entity.status != null and ew.entity.status != ''">
            AND status = #{ew.entity.status}
        </if>
    </where>
    ORDER BY created_at DESC
</select>

    <!-- 更新请假状态 -->
    <update id="updateLeaveStatus">
        UPDATE leave_request
        SET status = #{status},
            approval_level = #{approvalLevel},
            updated_at = NOW()
        WHERE id = #{requestId}
    </update>

</mapper>
